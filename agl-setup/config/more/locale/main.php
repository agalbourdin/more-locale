<?php
return array(

    /**
     * Default language of your application.
     */
    'default' => 'en',

    /**
     * Display default language code in URLs?
     */
    'show_default_in_url' => false,

    /**
     * List of all application's accepted languages codes.
     */
    'accepted' => array(
        'en'
    ),

    /**
     * Optional: used to declare specific domain names for some languages.
     *
     * Key: domaine name
     * Value: language code
     *
     * For example:
     * 'domain.com' => 'en'
     * 'domain.fr'  => 'fr'
     *
     * In this case, languages codes will obviously not be added to URLs.
     */
    'domains' => array(

    ),

    /**
     * Optional: list URLs parts ("module/view" syntax) that you wish to
     * translate, as GetText values.
     *
     * For example:
     * _("home/index")
     * _("home/download")
     *
     * Then translate those strings into your GetText files. URLs will be
     * automatically translated.
     *
     * As reminder, URLs syntax is:
     * {module}/{view}/{param_key1}/{param_value1}/{param_key2}/{param_value2}/
     */
    'urls' => array(

    ),

    /**
     * Optional: list params keys that you wish to translate, as GetText values.
     *
     * For example:
     * _("user")
     *
     * Then translate those strings into your GetText files. Params keys will be
     * automatically translated.
     *
     * As reminder, URLs syntax is:
     * {module}/{view}/{param_key1}/{param_value1}/{param_key2}/{param_value2}/
     */
    'params' => array(

    ),

    /**
     * Optional: list params values that you wish to translate, as GetText
     * values.
     *
     * For example:
     * _("how-to")
     *
     * Then translate those strings into your GetText files. Params values will
     * be automatically translated.
     *
     * As reminder, URLs syntax is:
     * {module}/{view}/{param_key1}/{param_value1}/{param_key2}/{param_value2}/
     */
    'values' => array(

    )

);
